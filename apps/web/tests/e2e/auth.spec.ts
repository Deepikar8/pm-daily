import { test } from "@playwright/test";

test.skip("user can request magic link and sees confirmation", async ({ page: _page }) => {
  // TODO: implement once /signin landing page exists (Phase 5)
  // 1. await page.goto("/");
  // 2. await page.click("text=Sign in");
  // 3. await page.fill('input[name="email"]', "test@example.com");
  // 4. await page.click('button[type="submit"]');
  // 5. await expect(page.getByText(/check your email/i)).toBeVisible();
});
